<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lh.mapper.NewsMapper">
    <select id="findAllNews" resultType="com.lh.pojo.News">
        select *
        from t_news
    </select>
    <select id="findNewsByID"  parameterType="Integer" resultType="com.lh.pojo.News">
        select *
        from t_news
        where id = #{id}
    </select>
    <insert id="insertNews" parameterType="com.lh.pojo.News">
        insert into t_news(title,author,create_date,content,image_path)
        values(#{title},#{author},#{create_date},#{content},#{image_path})
    </insert>
    <update id="updateNews" parameterType="com.lh.pojo.News">
        update t_news
        set
            content=#{content},
            title=#{title},
            create_date=#{create_date},
            author=#{author},
            image_path=#{image_path}
        where
            id = #{id}
    </update>
    <delete id="deleteNewsByID" parameterType="integer">
        delete
        from t_news
        where id=#{id}
    </delete>
    <select id="findNewsByStr" parameterType="java.lang.String" resultType="com.lh.pojo.News">
        select *
        from t_news
        where 1=1
        <if test="searchStr!=null and searchStr!=''">
            and (title like CONCAT('%',#{searchParam},'%')
            or author like CONCAT('%',#{searchParam},'%')
            or create_date like CONCAT('%',#{searchParam},'%'))
        </if>
    </select>
</mapper>